export class GameConsoleSpan {
  gameSpanEstilo: string;
  gameType: string;

  constructor(gameSpanEstilo: string, gameType: string) {
    this.gameSpanEstilo = gameSpanEstilo;
    this.gameType = gameType;
  }
}
